# Server Config
server.port=${PORT:8080}                # Use Railway's assigned port or default to 8080
server.address=0.0.0.0                  # Bind to all interfaces

# DB Configuration (loaded from Railway environment variables)
#spring.datasource.url=${DB_URL}
#spring.datasource.username=${DB_USERNAME}
#spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.datasource.url=jdbc:mysql://root:bxQurPiLfkeydwbdCquSnvvxyorlnbmH@metro.proxy.rlwy.net:41150/railway
spring.datasource.username=root
spring.datasource.password=bxQurPiLfkeydwbdCquSnvvxyorlnbmH

# JPA Config
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Thymeleaf Config
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# Logging Config
logging.level.org.springframework.boot.autoconfigure=warn

# Profiles
spring.profiles.active=prod
